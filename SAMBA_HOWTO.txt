╔══════════════════════════════════════════════════════════════════╗
║     COMPARTIR CARPETA POR SAMBA - GUÍA COMPLETA                  ║
║     Subir Fotos al Álbum Familiar desde Windows/Mac/Linux        ║
╚══════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════
📋 ÍNDICE
═══════════════════════════════════════════════════════════════════

1. ¿Qué es Samba y para qué sirve?
2. Instalación y Configuración en Odroid
3. Conectar desde Windows
4. Conectar desde Mac
5. Conectar desde Linux
6. Subir Fotos Paso a Paso
7. Troubleshooting
8. Seguridad y Permisos
9. Comandos de Referencia Rápida

═══════════════════════════════════════════════════════════════════
1. ¿QUÉ ES SAMBA Y PARA QUÉ SIRVE?
═══════════════════════════════════════════════════════════════════

Samba permite compartir carpetas del Odroid en la red local, accesibles
desde Windows, Mac o Linux como si fueran carpetas locales.

VENTAJAS:
  ✅ Copiar fotos con arrastrar y soltar (drag & drop)
  ✅ No necesitas comandos SCP ni SFTP
  ✅ Funciona igual que carpetas de red de Windows
  ✅ Puedes acceder desde explorador de archivos
  ✅ Soporta copias masivas de fotos

WORKFLOW:
  Cámara/USB → PC → Copiar a Samba → Odroid → Scripts de álbum


═══════════════════════════════════════════════════════════════════
2. INSTALACIÓN Y CONFIGURACIÓN EN ODROID
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
PASO 1: Instalar Samba
══════════════════════════════════════════════════════════════════

Conectarse por SSH al Odroid:

    ssh dietpi@192.168.1.147

Instalar Samba:

    sudo apt update
    sudo apt install samba samba-common-bin -y

Verificar instalación:

    smbd --version
    # Debería mostrar: Version 4.x.x


══════════════════════════════════════════════════════════════════
PASO 2: Crear Usuario Samba
══════════════════════════════════════════════════════════════════

Crear password para el usuario dietpi en Samba:

    sudo smbpasswd -a dietpi

Te pedirá una contraseña. Usa una fácil de recordar, por ejemplo:

    Password: Familia2024

IMPORTANTE: Este password es SOLO para Samba, diferente al SSH.


══════════════════════════════════════════════════════════════════
PASO 3: Configurar Compartición
══════════════════════════════════════════════════════════════════

Hacer backup de la configuración original:

    sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup

Editar configuración:

    sudo nano /etc/samba/smb.conf

Agregar AL FINAL del archivo:

┌─────────────────────────────────────────────────────────────────┐
│ COPIAR ESTO AL FINAL DE /etc/samba/smb.conf                    │
└─────────────────────────────────────────────────────────────────┘

[Album-Fotos]
   comment = Carpeta para subir fotos al álbum familiar
   path = /home/dietpi/album_familiar/fotos_para_subir
   browseable = yes
   read only = no
   writable = yes
   valid users = dietpi
   create mask = 0644
   directory mask = 0755
   force user = dietpi
   force group = dietpi

┌─────────────────────────────────────────────────────────────────┐
│ Explicación de parámetros:                                      │
│                                                                 │
│ [Album-Fotos]      → Nombre que verás en la red                │
│ path               → Carpeta compartida                         │
│ browseable         → Visible en explorador de red              │
│ writable           → Permite escritura                          │
│ valid users        → Solo usuario dietpi puede acceder         │
│ create mask        → Permisos de archivos nuevos (rw-r--r--)   │
│ directory mask     → Permisos de carpetas nuevas (rwxr-xr-x)   │
│ force user/group   → Dueño de archivos creados                 │
└─────────────────────────────────────────────────────────────────┘

Guardar archivo:
    Ctrl+O  → Enter → Ctrl+X


══════════════════════════════════════════════════════════════════
PASO 4: Verificar Configuración
══════════════════════════════════════════════════════════════════

Probar que no hay errores de sintaxis:

    testparm

Debería mostrar:
    Loaded services file OK.
    Server role: ROLE_STANDALONE


══════════════════════════════════════════════════════════════════
PASO 5: Reiniciar Servicios Samba
══════════════════════════════════════════════════════════════════

    sudo systemctl restart smbd
    sudo systemctl restart nmbd

Habilitar inicio automático:

    sudo systemctl enable smbd
    sudo systemctl enable nmbd

Verificar estado:

    sudo systemctl status smbd

Debería mostrar: "active (running)" en verde


══════════════════════════════════════════════════════════════════
PASO 6: Configurar Firewall (si está activo)
══════════════════════════════════════════════════════════════════

Verificar si el firewall está activo:

    sudo ufw status

Si muestra "Status: active", permitir Samba:

    sudo ufw allow Samba

O específicamente los puertos:

    sudo ufw allow 139/tcp
    sudo ufw allow 445/tcp
    sudo ufw allow 137/udp
    sudo ufw allow 138/udp

Recargar firewall:

    sudo ufw reload


══════════════════════════════════════════════════════════════════
PASO 7: Verificar Acceso en Red
══════════════════════════════════════════════════════════════════

Desde el mismo Odroid, verificar que Samba esté escuchando:

    sudo netstat -tuln | grep -E '139|445'

Debería mostrar algo como:
    tcp  0.0.0.0:139  LISTEN
    tcp  0.0.0.0:445  LISTEN


═══════════════════════════════════════════════════════════════════
3. CONECTAR DESDE WINDOWS
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
MÉTODO 1: Explorador de Archivos (Recomendado)
══════════════════════════════════════════════════════════════════

1. Abrir Explorador de Archivos (Windows + E)

2. En la barra de direcciones, escribir:

    \\192.168.1.147\Album-Fotos

   O también:

    \\odroid-c2\Album-Fotos

3. Presionar Enter

4. Te pedirá credenciales:
    Usuario:     dietpi
    Contraseña:  Familia2024  (la que pusiste en smbpasswd)

5. Marcar "Recordar credenciales" si quieres

6. ✅ ¡Listo! Verás la carpeta como cualquier carpeta de red


══════════════════════════════════════════════════════════════════
MÉTODO 2: Mapear Unidad de Red (Permanente)
══════════════════════════════════════════════════════════════════

Para tener la carpeta siempre disponible como una unidad (Z:, Y:, etc.):

1. Explorador de Archivos → Clic derecho en "Este equipo"

2. "Conectar a unidad de red"

3. Configurar:
    Unidad:    Z: (o la letra que prefieras)
    Carpeta:   \\192.168.1.147\Album-Fotos
    
    ☑ Volver a conectar al iniciar sesión
    ☑ Conectar con credenciales diferentes (si es necesario)

4. Click "Finalizar"

5. Ingresar credenciales:
    Usuario:     dietpi
    Contraseña:  Familia2024

6. ✅ Ahora tendrás una unidad Z: permanente


══════════════════════════════════════════════════════════════════
MÉTODO 3: Comando (Línea de Comandos)
══════════════════════════════════════════════════════════════════

Abrir CMD o PowerShell:

    net use Z: \\192.168.1.147\Album-Fotos /user:dietpi Familia2024

Para desconectar:

    net use Z: /delete


═══════════════════════════════════════════════════════════════════
4. CONECTAR DESDE MAC
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
MÉTODO 1: Finder (Recomendado)
══════════════════════════════════════════════════════════════════

1. Abrir Finder

2. Menú "Ir" → "Conectarse al servidor" (Cmd + K)

3. En "Dirección del servidor" escribir:

    smb://192.168.1.147/Album-Fotos

4. Click "Conectar"

5. Seleccionar "Usuario registrado"

6. Ingresar credenciales:
    Nombre:      dietpi
    Contraseña:  Familia2024

7. ✅ El recurso aparecerá en el Finder


══════════════════════════════════════════════════════════════════
MÉTODO 2: Terminal
══════════════════════════════════════════════════════════════════

    open smb://dietpi:Familia2024@192.168.1.147/Album-Fotos


═══════════════════════════════════════════════════════════════════
5. CONECTAR DESDE LINUX
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
MÉTODO 1: Gestor de Archivos (GNOME/KDE/XFCE)
══════════════════════════════════════════════════════════════════

En Nautilus (Ubuntu/GNOME):

1. Abrir "Archivos"

2. Barra lateral → "Otras ubicaciones"

3. En "Conectar al servidor" escribir:

    smb://192.168.1.147/Album-Fotos

4. Click "Conectar"

5. Ingresar:
    Usuario:     dietpi
    Contraseña:  Familia2024

6. ✅ Montado en /run/user/1000/gvfs/...


══════════════════════════════════════════════════════════════════
MÉTODO 2: Montar Manualmente (Terminal)
══════════════════════════════════════════════════════════════════

Instalar cifs-utils (si no está):

    sudo apt install cifs-utils

Crear punto de montaje:

    sudo mkdir -p /mnt/album-fotos

Montar:

    sudo mount -t cifs //192.168.1.147/Album-Fotos /mnt/album-fotos \
        -o username=dietpi,password=Familia2024

Verificar:

    ls /mnt/album-fotos

Desmontar:

    sudo umount /mnt/album-fotos


══════════════════════════════════════════════════════════════════
MÉTODO 3: Montaje Automático al Inicio (fstab)
══════════════════════════════════════════════════════════════════

Crear archivo de credenciales:

    sudo nano /root/.smbcredentials

Contenido:

    username=dietpi
    password=Familia2024

Proteger archivo:

    sudo chmod 600 /root/.smbcredentials

Editar fstab:

    sudo nano /etc/fstab

Agregar al final:

    //192.168.1.147/Album-Fotos /mnt/album-fotos cifs credentials=/root/.smbcredentials,uid=1000,gid=1000 0 0

Montar sin reiniciar:

    sudo mount -a


═══════════════════════════════════════════════════════════════════
6. SUBIR FOTOS PASO A PASO
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
WORKFLOW COMPLETO
══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ PASO 1: Organizar Fotos en tu PC                               │
└─────────────────────────────────────────────────────────────────┘

Crea carpetas por evento en tu PC local:

    MiPC/Fotos/
    ├── 2025/
    │   ├── Cumpleaños_Juan/
    │   │   ├── IMG_001.jpg
    │   │   ├── IMG_002.jpg
    │   │   └── IMG_003.jpg
    │   ├── Vacaciones_Playa/
    │   └── Navidad/
    └── 2026/


┌─────────────────────────────────────────────────────────────────┐
│ PASO 2: Conectar a la Carpeta Compartida Samba                 │
└─────────────────────────────────────────────────────────────────┘

Windows:  \\192.168.1.147\Album-Fotos
Mac:      smb://192.168.1.147/Album-Fotos
Linux:    smb://192.168.1.147/Album-Fotos


┌─────────────────────────────────────────────────────────────────┐
│ PASO 3: Crear Subcarpetas en Samba                             │
└─────────────────────────────────────────────────────────────────┘

Dentro de \\192.168.1.147\Album-Fotos crear:

    Album-Fotos/
    ├── 2025/
    │   ├── Cumpleaños_Juan/
    │   ├── Vacaciones_Playa/
    │   └── Navidad/
    └── 2026/


┌─────────────────────────────────────────────────────────────────┐
│ PASO 4: Copiar Fotos (Drag & Drop)                             │
└─────────────────────────────────────────────────────────────────┘

Simplemente arrastra las fotos desde tu PC a la carpeta Samba:

    [Tu PC] → [Seleccionar fotos] → [Arrastrar] → [Album-Fotos/2025/Cumpleaños_Juan/]

TIPS:
  ✅ Puedes copiar MUCHAS fotos a la vez
  ✅ Puedes copiar carpetas completas
  ✅ La velocidad depende de tu red (WiFi vs Ethernet)


┌─────────────────────────────────────────────────────────────────┐
│ PASO 5: Procesar Fotos en el Odroid                            │
└─────────────────────────────────────────────────────────────────┘

Conectarse por SSH al Odroid:

    ssh dietpi@192.168.1.147

Verificar que las fotos llegaron:

    ls ~/album_familiar/fotos_para_subir/2025/Cumpleaños_Juan/

Procesar las fotos:

    cd ~/album_familiar
    ./scripts/batch_upload.sh

Cuando pregunte la ruta:

    fotos_para_subir/2025/Cumpleaños_Juan

Título base:

    Cumpleaños de Juan

Descripción:

    Celebración familiar


┌─────────────────────────────────────────────────────────────────┐
│ PASO 6: Desplegar (Opcional)                                   │
└─────────────────────────────────────────────────────────────────┘

Si quieres desplegar inmediatamente:

    ./scripts/deploy.sh

O espera al deployment automático del domingo.


┌─────────────────────────────────────────────────────────────────┐
│ PASO 7: Limpiar Carpeta Samba (Opcional)                       │
└─────────────────────────────────────────────────────────────────┘

Una vez procesadas, puedes borrar las fotos de la carpeta Samba
para liberar espacio (las fotos procesadas ya están en static/images/).

Desde el Odroid:

    rm -rf ~/album_familiar/fotos_para_subir/2025/Cumpleaños_Juan/*

O desde Windows/Mac:
    Simplemente borrar la carpeta desde el explorador.


═══════════════════════════════════════════════════════════════════
7. TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
PROBLEMA: No puedo ver \\192.168.1.147 en la red
══════════════════════════════════════════════════════════════════

SOLUCIÓN 1: Verificar que Samba esté corriendo

En el Odroid:

    sudo systemctl status smbd

Si no está corriendo:

    sudo systemctl start smbd
    sudo systemctl start nmbd


SOLUCIÓN 2: Verificar firewall

En el Odroid:

    sudo ufw status

Si está bloqueando, permitir Samba:

    sudo ufw allow Samba


SOLUCIÓN 3: Usar IP directamente

En lugar de esperar detección automática, escribe la ruta completa:

    \\192.168.1.147\Album-Fotos


══════════════════════════════════════════════════════════════════
PROBLEMA: "Acceso denegado" o "Credenciales inválidas"
══════════════════════════════════════════════════════════════════

SOLUCIÓN 1: Verificar usuario Samba

En el Odroid:

    sudo pdbedit -L

Debería listar "dietpi"

Si no está, crearlo:

    sudo smbpasswd -a dietpi


SOLUCIÓN 2: Resetear password

    sudo smbpasswd dietpi

Ingresar nueva contraseña.


SOLUCIÓN 3: Windows Credential Manager

Windows puede tener credenciales guardadas incorrectas.

1. Panel de Control → Administrador de Credenciales
2. Credenciales de Windows
3. Buscar "192.168.1.147" o "odroid"
4. Eliminar
5. Reintentar conexión


══════════════════════════════════════════════════════════════════
PROBLEMA: Puedo ver la carpeta pero no puedo escribir
══════════════════════════════════════════════════════════════════

SOLUCIÓN: Verificar permisos de la carpeta

En el Odroid:

    ls -la ~/album_familiar/ | grep fotos_para_subir

Debería mostrar:
    drwxr-xr-x ... dietpi dietpi ... fotos_para_subir

Si no es así:

    sudo chown -R dietpi:dietpi ~/album_familiar/fotos_para_subir
    chmod 755 ~/album_familiar/fotos_para_subir


══════════════════════════════════════════════════════════════════
PROBLEMA: Muy lento al copiar fotos
══════════════════════════════════════════════════════════════════

CAUSAS COMUNES:
  ❌ WiFi con señal débil
  ❌ Router de baja calidad
  ❌ Muchos dispositivos en la red

SOLUCIONES:
  ✅ Usar cable Ethernet en lugar de WiFi
  ✅ Copiar fotos en lotes más pequeños (10-20 a la vez)
  ✅ Verificar que nadie más esté usando la red
  ✅ Cerrar otras apps que usen red (streaming, descargas)

VELOCIDADES ESPERADAS:
  WiFi 2.4GHz:  2-5 MB/s  (lento)
  WiFi 5GHz:    10-30 MB/s (medio)
  Ethernet:     50-100 MB/s (rápido)


══════════════════════════════════════════════════════════════════
PROBLEMA: "No se puede conectar al recurso compartido"
══════════════════════════════════════════════════════════════════

SOLUCIÓN: Verificar conectividad de red

Desde tu PC:

    ping 192.168.1.147

Si no responde, problema de red.

Verificar que estés en la misma red:

    # Windows
    ipconfig

    # Mac/Linux
    ifconfig

Tu IP debe estar en el mismo rango (ej: 192.168.1.x)


═══════════════════════════════════════════════════════════════════
8. SEGURIDAD Y PERMISOS
═══════════════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════════════
BUENAS PRÁCTICAS
══════════════════════════════════════════════════════════════════

✅ Usar password fuerte para Samba
✅ NO compartir Samba fuera de tu red local
✅ Configurar firewall para bloquear acceso externo
✅ Mantener Samba actualizado

    sudo apt update && sudo apt upgrade samba


══════════════════════════════════════════════════════════════════
LIMITAR ACCESO POR IP (Opcional)
══════════════════════════════════════════════════════════════════

Si solo quieres que ciertos dispositivos accedan:

Editar /etc/samba/smb.conf:

    sudo nano /etc/samba/smb.conf

En la sección [Album-Fotos] agregar:

    hosts allow = 192.168.1.0/24 127.0.0.1
    hosts deny = 0.0.0.0/0

Esto permite solo dispositivos en tu red local (192.168.1.x)


══════════════════════════════════════════════════════════════════
VER QUIÉN ESTÁ CONECTADO
══════════════════════════════════════════════════════════════════

    sudo smbstatus

Muestra:
  - Usuarios conectados
  - Recursos compartidos en uso
  - Archivos abiertos


═══════════════════════════════════════════════════════════════════
9. COMANDOS DE REFERENCIA RÁPIDA
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ EN EL ODROID (SSH)                                              │
└─────────────────────────────────────────────────────────────────┘

# Reiniciar Samba
sudo systemctl restart smbd nmbd

# Ver estado
sudo systemctl status smbd

# Ver logs
sudo tail -f /var/log/samba/log.smbd

# Ver usuarios Samba
sudo pdbedit -L

# Cambiar password Samba
sudo smbpasswd dietpi

# Probar configuración
testparm

# Ver conexiones activas
sudo smbstatus

# Ver permisos de carpeta
ls -la ~/album_familiar/fotos_para_subir


┌─────────────────────────────────────────────────────────────────┐
│ DESDE WINDOWS                                                   │
└─────────────────────────────────────────────────────────────────┘

# Conectar
\\192.168.1.147\Album-Fotos

# Mapear unidad
net use Z: \\192.168.1.147\Album-Fotos /user:dietpi Familia2024

# Desconectar
net use Z: /delete

# Ver conexiones
net use


┌─────────────────────────────────────────────────────────────────┐
│ DESDE MAC                                                       │
└─────────────────────────────────────────────────────────────────┘

# Conectar desde Terminal
open smb://192.168.1.147/Album-Fotos

# Conectar con credenciales
open smb://dietpi:Familia2024@192.168.1.147/Album-Fotos


┌─────────────────────────────────────────────────────────────────┐
│ DESDE LINUX                                                     │
└─────────────────────────────────────────────────────────────────┘

# Montar
sudo mount -t cifs //192.168.1.147/Album-Fotos /mnt/album-fotos \
    -o username=dietpi,password=Familia2024

# Desmontar
sudo umount /mnt/album-fotos

# Listar recursos compartidos
smbclient -L 192.168.1.147 -U dietpi


═══════════════════════════════════════════════════════════════════
📊 RESUMEN RÁPIDO
═══════════════════════════════════════════════════════════════════

1. INSTALAR EN ODROID:
   sudo apt install samba -y
   sudo smbpasswd -a dietpi

2. CONFIGURAR:
   sudo nano /etc/samba/smb.conf
   [Agregar sección Album-Fotos]
   sudo systemctl restart smbd nmbd

3. CONECTAR DESDE TU PC:
   Windows: \\192.168.1.147\Album-Fotos
   Mac:     smb://192.168.1.147/Album-Fotos
   Linux:   smb://192.168.1.147/Album-Fotos

4. COPIAR FOTOS:
   Arrastrar y soltar a la carpeta compartida

5. PROCESAR:
   ssh dietpi@192.168.1.147
   cd ~/album_familiar
   ./scripts/batch_upload.sh

═══════════════════════════════════════════════════════════════════

¡Ahora puedes copiar fotos fácilmente sin usar comandos! 📸✨

═══════════════════════════════════════════════════════════════════
